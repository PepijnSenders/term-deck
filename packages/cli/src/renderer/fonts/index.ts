/**
 * Embedded fonts for figlet text generation
 *
 * These fonts are bundled with the application to ensure they're available
 * without requiring external file loading at runtime.
 */
export { STANDARD_FONT } from './standard.js'
